/**
 * @todo
 */

Drupal.omega = Drupal.omega || {};

(function($) {
  
  /**
   * @todo
   */
  Drupal.behaviors.sampleFrontendFunction = {
    attach: function (context) {
      
    }
  };
})(jQuery);
